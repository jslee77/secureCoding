# 3. 결과 보고서 — 검증 결과

> 문서 순서: [1. 문제 진단](01-문제진단.md) → [2. 개선 내역](02-개선내역.md) → **[3. 결과 보고서](03-결과보고서.md)** → [4. 추가 재점검](04-추가재점검.md)

작성: 2026-09-22 · 갱신: 2026-09-23 · 대상: 현재 소스, 테스트, 설정, 배포 예제

초기 V-01~V-23, 후속 R-01~R-10에 이어 재점검 S-01~S-11과 운영 완결성 O-01~O-06을 수정했습니다. **S/O 진단 항목의 코드 조치를 반영하고 아래 범위에서 검증했습니다.** 이것이 모든 가능한 취약점의 부재나 운영 배포 인증을 의미하지는 않습니다.

과거의 “코드 결함 없음 / 운영 인프라만 남음”이라는 표현은 철회합니다. 초기 진단은 [문제 진단](01-문제진단.md), 수정 원리는 [개선 내역](02-개선내역.md), 수정 전 재현 증거는 [추가 재점검](04-추가재점검.md)에 있습니다.

## 1. 검증 결과

| 검사 | 결과 | 범위와 한계 |
|---|---|---|
| Python 회귀 테스트 | **150 passed**, Linux 컨테이너·Python 3.12.14 | 기존 85개 + S 재점검 46개 + 운영/재시드 19개; 임시 SQLite·파일·키 사용 |
| 프론트 보안 테스트 | **3 passed**, Node 25.9.0 | 실제 app.js를 Node VM에서 실행; DOM·fetch 대역 사용 |
| 의존성 감사 | **30개 패키지, 알려진 취약점 0건**, 생략 0건 | `pip-audit -r requirements.txt`, 전이 의존성 포함 |
| 정적 분석(bandit) | **1,507 LOC, High/Medium/Low 0건** | `bandit 1.9.4 -r app/`; 초기 10건은 모두 오탐/설계상 안전으로 분류 후 사유를 단 `# nosec`로 억제 |
| JavaScript 문법 | `node --check static/app.js` 통과 | 실제 브라우저 통합 테스트와 구분 |
| 실제 서버 이미지 | 비루트 UID 10001·gunicorn 기동, HTTP/CSP/no-store, 데모 시드 ON/OFF에 따른 로그인, `.env`·`.venv` 부재 확인 | 임시 컨테이너·tmpfs·임의 루프백 포트 사용; 기존 데이터 볼륨 미사용 |
| 문서/운영 명령 | 문서 6개·로컬 링크/앵커 118개, JSON 예제 13개, Bash 문법, Compose 변수 전달·우선순위 확인 | 외부 링크 가용성·모든 브라우저 동작을 보장하지 않음 |
| 변경 형식 | `git diff --check` 통과 | 공백·패치 형식 |

[의존성 감사 원본](security-review/dependency-audit.json), [bandit 리포트(HTML)](security-review/bandit-report.html)·[JSON](security-review/bandit.json), [검증 기록](security-review/verification.txt), [추가 회귀 테스트](../tests/test_review_fixes.py), [프론트 테스트](../tests/frontend.test.cjs), [운영 명령 테스트](../tests/test_operations.py), [재시드 테스트](../tests/test_reseed.py), [컨테이너 기동 검증](security-review/container_smoke.py).

### bandit 정적 분석 — 초기 10건 분류

파이썬 정적 보안 분석기 bandit을 `app/` 전체에 적용했습니다. 최초 스캔의 10건(Medium 2·Low 8)은 실제 취약점이 아니라 **오탐 또는 이미 방어된 설계 결정**이었고, 각 지점에 근거를 단 `# nosec <ID>` 주석으로 억제했습니다. 이후 스캔은 0건이며, CI가 앞으로 새로 생기는 지적을 차단합니다.

| 규칙 | 위치 | 판정 | 근거 |
|---|---|---|---|
| B608 (SQL) | `documents.py` · `update_document`/`delete_document` | 오탐 | 컬럼·테이블명은 고정 화이트리스트, 값은 파라미터 바인딩 |
| B603 (subprocess) | `files.py` · 업로드 검증, `tools.py` · PDF 변환 | 설계상 안전 | `shell=False` + 분리된 인자; 실행 파일은 신뢰된 서버 설정, 경로·확장자는 서버 생성/검증. 파서 자체의 안전성까지 보장하는 억제는 아님 |
| B404 (import subprocess) | `files.py`, `tools.py` | 정보성 | 위 두 용도로만 사용 |
| B105 (hardcoded password) | `validation.py` · `STRING_LIMITS` | 오탐 | 길이 제한 dict의 키 이름(`password` 등)일 뿐 시크릿 아님 |

> `# nosec`는 지정한 규칙만 억제합니다. 같은 규칙에 해당하는 새 결함은 억제 때문에 보이지 않을 수 있고, 다른 규칙도 분석기가 탐지할 수 있는 패턴만 보고합니다. 0건은 수동 검토나 억제 사유 재검토를 대체하지 않습니다.

CI는 Python 3.12와 Node 24에서 테스트하고 의존성 감사를 수행하도록 설정했습니다. 위 수치는 로컬 Docker 및 호스트 Node에서 실제 수행한 결과입니다. CI의 원격 실행 결과와 동일하다고 미리 가정하지 않습니다. Linux 이미지의 실제 gunicorn 기동은 검증했지만 TLS 프록시·브라우저 전체 흐름은 검증하지 않았습니다. 기존 SSRF와 PDF 내보내기 테스트는 모의 네트워크/변환기를 사용합니다. 새 업로드 검사는 실제 Pillow·pypdf 파서를 실행합니다.

## 2. 재점검 항목별 조치

| ID | 수정한 동작 | 검증 |
|---|---|---|
| S-01 | 비밀번호 변경·재설정에서 비밀번호, JWT 세대, 기존 복구 토큰 폐기를 한 트랜잭션으로 처리 | 일반 변경 후 이전 재설정 토큰 `400` |
| S-02 | `BEGIN IMMEDIATE` 후 유효 토큰 재조회, 같은 사용자의 모든 토큰 소진·비밀번호 변경 원자화 | 같은 토큰/서로 다른 동일 계정 토큰의 병렬 요청에서 한 건만 `200`; 중간 실패 롤백 |
| S-03 | 공개 범위 변경은 소유자만 허용 | 편집자는 본문 수정 가능, 공개 변경 `403`, 제3자 접근 `404` |
| S-04 | 중복 공유 정리, 복합 유일성 인덱스, UPSERT, 소유자 전용 공유 목록·삭제 | 강등 후 편집 `403`; 회수 후 문서·댓글·첨부 접근 차단 |
| S-05 | 로그인·가입에도 CSRF 헤더 요구, Origin/Fetch Metadata 확인, JSON Content-Type 강제 | 헤더 누락/외부 출처 `403`, text/plain `415` |
| S-06 | 공통 JSON 객체·필드 타입·길이 검증, 엄격한 boolean, 주민번호 형식 검증·고정 마스킹 | 잘못된 타입 `400`; 문자열 false로 권한 부여 불가 |
| S-07 | 계정/IP 제한, 문서·댓글·첨부 개수 및 크기 제한, 저장 총량, 목록·검색 페이지네이션 | IP를 바꿔도 비밀번호 확인 6번째 `429`; 한도 초과 `400/409/413` |
| S-08 | 문서 삭제 시 자식 행 삭제와 파일 삭제 큐를 함께 커밋; 실패 파일은 재시도 | 관련 행·파일 제거, unlink 실패 후 큐 보존·재실행, 업로드 DB 실패 파일 정리 |
| S-09 | 파일 오류 시 시작 중단; 잠금·0600 임시 파일·fsync·원자적 교체; 키 형식/길이 확인 | 손상 파일 보존, 병렬 생성 키 일치, 잘못된 권한/짧은 키 거부 |
| S-10 | 모든 API 응답 no-store; 로그아웃 화면·입력 초기화, 이전 세션 응답 무시 | 응답 헤더 검사; 지연 응답이 로그아웃 후 개인정보를 다시 표시하지 않음 |
| S-11 | 확장자에 맞는 실제 이미지/PDF/텍스트 파싱을 별도 제한 프로세스에서 수행 | 가짜 파일 `400`, 정상 4개 형식 성공, 검증 시간 초과 거부 |

추가로 refresh 토큰 회전도 원자화했습니다. 기본 교육용 API는 비활성화했고, 정적 파일을 명시적 허용목록으로 제한해 구버전 `static/uploads/` 잔류 파일의 직접 노출을 차단했습니다. 공유받은 문서도 목록·검색에 표시하며, 화면에는 페이지 이동을 제공합니다. Compose는 루프백에 바인딩하고 기본 gunicorn 워커는 memory 레이트리밋에 맞게 1개로 변경했습니다.

### 운영 완결성 추가 조치 (O-01~O-06)

| ID | 수정·문서 동기화 | 검증 |
|---|---|---|
| O-01 | `.env` 계열·로컬 가상환경을 Docker 빌드에서 제외, 임시 시크릿 Git 제외 | 실제 이미지에 `.env`·`.venv` 없음 |
| O-02 | 로그 명령 실행, 검사 실패 전파, Node 필수화, 상태 실패 코드, 헬스 대기 시간 제한 | 운영 명령 대역 테스트 + 실제 전체 검증 스크립트 |
| O-03 | Compose의 시드·교육 API·변환기 환경변수 전달 | Compose 설정 렌더링, 실제 서버 시드 ON/OFF |
| O-04 | 시크릿 생성 시점부터 `0600`, 원자적 저장, 기존 파일 보호; 새 설치 전용 안내 | 권한·키 형식·기존 파일/심볼릭 링크 보존 테스트 |
| O-05 | 일회용 컨테이너에서 스캔, 리포트 폴더만 쓰기 마운트, 실패 코드 유지 | 실제 HTML/JSON·의존성 JSON 생성 + 중간 실패 대역 테스트 |
| O-06 | 시드·신규 가입의 무작위 JWT 세대·첨부 삭제 큐 보존, 서비스 중지 후 초기화 | 이전 JWT `401`, 실패 파일/큐 보존·재시도, 실패 시 재기동 금지 |

상세 변경은 [개선 내역 §8](02-개선내역.md#8-운영-완결성-재점검-2026-09-22), 실행 절차는 [운영 문서](운영문서.md)를 참고하세요. 재시드는 파괴적 유지보수이며 DB 전체 초기화의 중간 실패를 자동 롤백하는 기능은 없습니다. 실패 시 중지 상태에서 백업 복구 또는 명시적 재초기화가 필요합니다.

## 3. 제한값과 정책

| 대상 | 기본값 |
|---|---|
| 요청 본문 | 16MiB |
| 비밀번호 | 새 비밀번호 최소 8자, 입력 최대 1,024자 |
| 문서 | 제목 200자, 본문 65,536자, 소유자당 1,000개 |
| 댓글 | 4,000자, 문서당 1,000개 |
| 첨부 | 파일당 8MiB, 업로더당 200개 / 100MiB |
| 목록·검색 | 기본 50개, 최대 100개, offset 최대 100,000 |
| 비밀번호 변경 | 계정당 분당 5회 + IP당 분당 20회 |
| 로그인 | 아이디당 분당 15회 + IP당 분당 30회 |
| 가입 / 복구 요청 / 복구 확인 | IP당 시간당 10회 / 분당 5회 / 분당 10회 |
| 일반 문서·댓글·파일·공유·도구·프로필 API | 기본 계정당 분당 120회; 명시적인 경로별 제한은 해당 정책 적용 |
| PDF 변환 / URL 미리보기 | 계정당 분당 3회 / 분당 10회 |
| 업로드 내용 검증 | 실제 시간 5초, CPU 3초, Linux 주소 공간 512MiB |
| 이미지 / PDF | 전체 프레임 합계 2천만 픽셀 / 암호화 PDF 거부·최대 100페이지 |

텍스트는 UTF-8이며 NUL 등 불필요한 제어문자를 거부합니다. 파일 파싱은 **악성코드 검사나 PDF의 능동 콘텐츠 제거(CDR)가 아닙니다.** 다운로드는 attachment와 nosniff를 유지합니다. 기본값은 예제 기준이며 실제 업무량·위험도에 따라 설정과 운영 자원 한도를 함께 조정해야 합니다.

## 4. 기존 데이터의 업그레이드

1. 서비스 중지 후 DB·업로드·암호화 키를 **같은 시점의 백업**으로 보존합니다. `python -m app.seed`는 초기화 명령이므로 운영 업그레이드에 사용하지 않습니다.
2. 시크릿 파일은 앱 실행 사용자 소유, 권한 `0600`이어야 합니다. 손상·누락 키를 새로 만들어 기존 암호문을 계속 사용하려 하지 말고 기존 키를 복구합니다. 기존 파일이 없는 신규 설치에서만 자동 생성합니다.
3. 앱은 새 컬럼·삭제 큐·인덱스를 생성합니다. 중복 공유는 **가장 큰 id(최근 요청)** 한 행을 남기므로 기존의 읽기 전용 변경 의도가 반영됩니다. 기존 첨부의 실제 파일 크기는 허용된 업로드 폴더에서 계산해 저장 한도에 포함합니다.
4. 이전 버전의 `static/uploads/`는 더 이상 HTTP로 제공하지 않습니다. 파일을 `instance/uploads/`로 옮기고 DB 매핑·소유권을 확인하는 작업은 백업 후 별도로 수행합니다. 구버전 XOR 암호문의 Fernet 변환이나 외부 키 교체도 자동 수행하지 않습니다.
5. 이미 남아 있는 고아 파일/행은 이번 삭제 API의 큐에 자동 등록되지 않습니다. 기존 저장소와 DB를 대조해 별도 정리합니다. 진행 중 프로세스 강제 종료까지 포함한 파일 시스템·DB의 완전한 원자성은 보장하지 않습니다.
6. 파일 삭제 실패는 `pending_file_deletions`에 남습니다. 앱 시작 때 재시도하며, 운영에서는 `flask --app 'app:create_app()' files cleanup`을 주기적으로 실행하고 남은 작업 수를 확인합니다. 삭제된 파일의 백업 보존기간은 별도 정책입니다.

## 5. 운영 배포에서 남는 작업

- 공개된 데모 계정이 있는 볼륨을 운영에 재사용하지 않습니다. `SEED_DEMO_DATA=0`은 이미 존재하는 계정을 삭제하지 않습니다. Compose의 미설정 기본값은 `1`이므로 운영에서는 `0`을 명시해야 합니다.
- `JWT_SECRET`·`SECRET_KEY`는 최소 32바이트의 강한 난수, `DATA_KEY`는 유효한 Fernet 키로 주입합니다. 키·데이터 백업, 복구 시험, 키 회전을 운영 절차로 관리합니다.
- `scripts/gen-secrets.sh`는 신규 설치 전용이며 `SEED_DEMO_DATA=0`을 생성합니다. 기존 DB에 적용할 때는 기존 `DATA_KEY`를 보존해야 합니다. `--force`는 `.env` 전체 교체이며 자동 재암호화 기능이 아닙니다.
- 빈 DB에서 회원가입은 일반 사용자만 만듭니다. 최초 운영 관리자 생성·승인 절차는 별도 구현이 필요하며 전용 부트스트랩 명령은 제공하지 않습니다.
- TLS 프록시, HTTP→HTTPS 전환, `COOKIE_SECURE=1`, 실제 프록시 수에 맞는 `TRUST_PROXY_HOPS`, 백엔드 직접 접근 차단을 적용합니다.
- 다중 워커/서버로 확장하면 Redis 서버·Python 클라이언트 의존성을 배포에 추가하고 공유 레이트리밋 저장소를 구성합니다. 기본 memory 카운터는 재시작하면 초기화됩니다. 이번에 다중 워커 Redis 통합 테스트를 수행한 것은 아닙니다.
- `ENABLE_TRAINING_ROUTES`는 기본 0입니다. 운영에서는 1로 설정하지 않습니다.
- 프로필 수정은 현재 네 필드 전체 교체이며, 생략하거나 빈 주민번호를 보내면 기존 값이 삭제됩니다. 웹 화면의 빈 주민번호 저장도 같습니다. 부분 수정·기존 주민번호 유지·삭제 의사 확인이 필요한 제품에는 API/UI 정책 보완이 필요합니다.
- `deliver_reset_token`은 발송 연동 지점이며 현재 로그만 기록합니다. 실제 계정 소유 확인 채널을 연결해야 복구 기능이 완성됩니다. 변경 가능한 미검증 이메일을 그대로 복구 대상으로 연결하지 않습니다.
- LibreOffice는 기본 이미지에 없습니다. PDF 변환이 필요하면 별도 자원 제한·네트워크 격리·동시성 제한을 갖춘 실행환경을 구성하고 실제 변환을 검증합니다. 변환기 미설치 시 `503`입니다.
- 개인정보 수집 최소화, 로그 회전/보존, 백업 파기, 파일 악성코드 검사 정책을 운영 요구에 맞춥니다. 사용되지 않는 API 토큰은 현재 인증 수단이 아니며, 향후 도입 시 해시 저장·권한 범위·만료·폐기를 설계해야 합니다.
- 직접 의존성은 버전 고정돼 있지만 전이 의존성 lock/hash, 이미지 digest, Actions commit pin은 아직 없습니다. 릴리스 재현성 강화와 정기 감사는 후속 공급망 관리 과제입니다.

백업 대상·환경변수 키 보존·복구 후 세션 처리와 로그 위치는 [운영 문서](운영문서.md#백업과-복구-범위)를 참고하세요. DB 복원은 재시드와 다르며, 이번 테스트는 실제 재해 복구 연습을 포함하지 않습니다.

## 6. 재검증 명령

```bash
pip install -r requirements-dev.txt
python -m pytest -q
node --test tests/frontend.test.cjs
python docs/security-review/probe_security.py
bandit -r app/                                   # 정적 보안 분석 (0건이어야 통과)
pip install pip-audit
pip-audit -r requirements.txt
```

Docker에서는 운영 서비스에 개발 도구를 설치하지 않고 일회용 테스트 컨테이너를 사용합니다. 런타임은 비루트이며 소스·패키지 경로에 쓰기 권한이 없습니다.

```bash
scripts/test.sh                 # Python 150개 + bandit + 의존성 감사 + 호스트 Node 3개
scripts/scan.sh --report        # bandit HTML/JSON + 의존성 JSON 갱신
python3 docs/security-review/container_smoke.py  # 위 명령이 만든 securedocs:test 이미지 검증
```

> 생성된 HTML 리포트 예시는 [docs/security-review/bandit-report.html](security-review/bandit-report.html)에 있습니다.

`probe_security.py`는 현재 `tests/test_review_fixes.py`를 실행하고 실패를 종료 코드에 반영합니다. 수정 전의 취약 동작은 `observations-before.jsonl`에만 역사적 증거로 남깁니다. CI가 통과해도 검증하지 않은 배포 조건까지 안전하다고 단정하지 않습니다.
