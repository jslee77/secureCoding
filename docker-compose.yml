services:
  securedocs:
    build: .
    ports:
      - "5000:5000"
    environment:
      # 로컬 확인용 데모 데이터. 운영에서는 지우고 시크릿(JWT_SECRET 등)을 주입한다.
      SEED_DEMO_DATA: "1"
    volumes:
      # 소스는 이미지에 고정하고, DB·업로드·시크릿만 볼륨으로 보존한다.
      - securedocs-data:/app/instance

volumes:
  securedocs-data:
